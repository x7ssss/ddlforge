{
  "name": "ddlforge-action",
  "version": "1.0.0",
  "description": "Bundled GitHub Action for ddlforge — Postgres migration lock linter",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "build:min": "ncc build src/index.ts -o dist --minify --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.8.2"
  }
}
